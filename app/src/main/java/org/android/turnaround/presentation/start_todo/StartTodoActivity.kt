package org.android.turnaround.presentation.start_todo

import android.os.Bundle
import org.android.turnaround.R
import org.android.turnaround.databinding.ActivityStartTodoBinding
import org.android.turnaround.util.binding.BindingActivity

class StartTodoActivity : BindingActivity<ActivityStartTodoBinding>(R.layout.activity_start_todo) {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
